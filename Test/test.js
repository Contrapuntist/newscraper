var assert = chai.assert;
var expect = require("chai").expect; 
var Article = require('../models/Article.js');
var scraperoute = require('../routes/scrapecontroller.js'); 
var appjs = require('../public/app.js');


// TEST: Click events are triggering properly  

describe('Scraping App', function() { 
    context('CLick events', function() { 
        it('on click of update button event should trigger database update', )
    })
}); 